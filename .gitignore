__pycache__

# Model files (large binaries, user-specific)
benchmark/models

# Benchmark individual run logs (the merged results.json is what matters)
benchmark/results/*.log
benchmark/results/system_info.json

# Keep results.json and results_new.json (system_info is embedded in these)
!benchmark/results/results.json
!benchmark/results/results_new.json

# Temporary/scratch files
*.tmp
*.bak

# Benchmark state file (used by is_benchmark_running.sh)
benchmark/.benchmark_state

# Old results format - causes merge conflicts and is superseded by results_new.jsonl.js
docs/results.json